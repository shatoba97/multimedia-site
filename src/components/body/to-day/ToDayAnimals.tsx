import React, { FC } from "react";
import { Route, Switch } from "react-router-dom";
import "./ToDayAnimals.scss";

const ToDayAnimals: React.FC<{}> = () => {
  const searchChange = (value: string) => {
    console.log(value);
  };

  return (
    <React.Fragment>
      
    </React.Fragment>
  );
};
export default ToDayAnimals;
